import React from "react";

type Props = {};

const CardButton = (props: Props) => {
  return (
    <button className="bg-background w-64 text-white h-12 rounded-lg text-sm">
      Buy now
    </button>
  );
};

export default CardButton;
